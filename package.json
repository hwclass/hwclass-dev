{
  "name": "hwclass-dev",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "now-build": "gatsby build",
    "prod": "run-s build serve",
    "deploy-redirect": "cd redirect && now && now --target production",
    "deploy-staging": "now --target staging",
    "deploy-site": "now && now --target production",
    "deploy": "run-s deploy-staging deploy-site deploy-redirect"
  },
  "dependencies": {
    "gatsby": "^2.15.28",
    "gatsby-plugin-google-analytics": "^2.1.20",
    "gatsby-plugin-offline": "^3.0.11",
    "gatsby-plugin-robots-txt": "^1.5.0",
    "gatsby-plugin-sitemap": "^2.2.16",
    "gatsby-theme-blog": "^1.0.2",
    "react": "^16.10.1",
    "react-dom": "^16.10.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
