import React, { Fragment } from "react"
import { Styled } from "theme-ui"

export default () => (
  <Fragment>
    <Styled.a href="http://hwclass.dev/">Baris Guler</Styled.a>
    {` `}
    <br />
    Web worker
  </Fragment>
)
